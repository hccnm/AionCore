# update

这是一个更新功能任务。

## 输入

- 已有原型目录
- 更新后的 PRD 文件

## 目标

根据变更后的 PRD，局部更新已有原型。

## 你要做的事

1. 对比 PRD 变化
2. 判断哪些页面需要更新
3. 更新 `prototype.json`
4. 只修改受影响的 `pages/*.html`
5. 在 `prototype.json.notes` 追加一条 `update` 记录

## 约束

- 不要整站重写
- 不要因为局部变更重命名大量页面
- 保持已有页面结构尽量稳定
- `notes` 要记录清楚本次更新改了哪些页面和原因

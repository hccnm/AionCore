# add

这是一个新增功能任务。

## 输入

- 已有原型目录
- 一个或多个新增 PRD 文件

## 目标

在不破坏已有原型的前提下，新增页面、模块或 flow。

## 你要做的事

1. 读取新增 PRD
2. 判断影响哪些页面
3. 更新 `prototype.json`
4. 只新增或修改受影响的 `pages/*.html`
5. 在 `prototype.json.notes` 追加一条 `add` 记录

## 约束

- 不要重做整个原型
- 不要改无关页面
- 继续保持页面自包含
- `notes` 要明确写出这次新增了什么和受影响页面
